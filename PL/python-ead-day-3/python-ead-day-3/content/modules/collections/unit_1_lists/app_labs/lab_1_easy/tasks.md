# Lab 1 Tasks: Basic Appointment Log

## Task 1: Initialize and Add
**Difficulty**: Easy | **Points**: 100

### Objective
Store patient names.

### Requirements
- Create a variable `appointments` as an empty list.
- Use `.append()` to add "Alice", "Bob", and "Charlie" in that order.
- Print the total number of appointments using `len()`.
