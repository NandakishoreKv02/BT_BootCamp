# Lab 1: Emergency Room Queue - Tasks

## Task 1: Arrival
In `arrive_patient`, use the `.append()` method to add the name to the list.

## Task 2: Admission
In `see_next_patient`:
- Check if the list has items.
- If it does, use `.pop(0)` to remove and return the first item.
- Else, return `None`.

## Task 3: Status
In `get_queue_length`, return the length of the list using `len()`.
