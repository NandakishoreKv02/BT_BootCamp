# Lab 3: Cross-Patient Allergy Tracker - Tasks

## Task 1: Type Conversion
In `get_common_allergies`, convert `group_a_list` and `group_b_list` into sets named `set_a` and `set_b`.

## Task 2: Find Intersection
Calculate the intersection of `set_a` and `set_b`.

## Task 3: Return Result
Return the resulting set.
