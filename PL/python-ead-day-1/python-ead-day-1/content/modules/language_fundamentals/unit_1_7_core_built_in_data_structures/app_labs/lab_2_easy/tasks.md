# Lab 2: Clinical Reference Ranges - Tasks

## Task 1: Static Return
Modify `get_hr_range` to return a tuple containing the numbers 60 and 100.

## Task 2: Unpacking
In `is_value_normal`, unpack the `reference_tuple` into two variables: `min_val` and `max_val`.

## Task 3: Comparison
Check if the input `value` is greater than or equal to `min_val` AND less than or equal to `max_val`. Return the resulting Boolean.
