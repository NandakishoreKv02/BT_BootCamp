# Lab 3: Multi-Vital Alert System - Tasks

## Task 1: Sub-Condition 1
Group the check for `temp > 39.0` and `spo2 < 92` using parentheses and the `and` operator.

## Task 2: Sub-Condition 2
Create the check for `is_conscious` being `False`. You can use `not is_conscious` or `is_conscious == False`.

## Task 3: Final Logic
Combine Task 1 and Task 2 with an `or` operator.
