# Lab 2: Lab Result History Navigator - Tasks

## Task 1: Direct Access
Attempt to return `results_list[offset]`.

## Task 2: Trap Out-of-Bounds
Wrap the access in a `try` block and catch `IndexError`.

## Task 3: Handle Failure
In the `except` block, return the string `"Result Not Available"`.
