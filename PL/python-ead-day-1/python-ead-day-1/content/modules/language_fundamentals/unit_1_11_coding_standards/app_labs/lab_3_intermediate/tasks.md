# Lab 3: The Pythonic Patient Portal - Tasks

## Task 1: Class Refactoring
Rename the class `patient_data` to `PatientData`.

## Task 2: Pythonic Search
In `is_admitted(patient_name, admission_list)`, delete the manual `while` loop logic. Replace it with a single `return ... in ...` statement.

## Task 3: Consistency Check
Ensure any instances created of the class use the new `PatientData` name.

## Task 4: Functional Verification
Test with a list of 10 names. Observe how much shorter and cleaner the Pythonic code is.
