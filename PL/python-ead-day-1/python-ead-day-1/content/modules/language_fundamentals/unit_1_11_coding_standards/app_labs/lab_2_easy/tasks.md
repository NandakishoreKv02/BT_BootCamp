# Lab 2: The Magic Number Mirror - Tasks

## Task 1: Define Constants
At the top level of the file, define:
- `MIN_NORMAL_HR = 60`
- `MAX_NORMAL_HR = 100`

## Task 2: Functional logic
In `check_vital_alert`, use the constants in your `if` condition instead of the raw numbers `60` and `100`.

## Task 3: Value Check
Test with `55` (should alert) and `75` (should be normal).
Observe how much easier it is to update the threshold by changing one value at the top of the file!
