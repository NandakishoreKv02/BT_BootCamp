# Lab 3 Tasks

## Task 1: The Sequence Seed
- Define `MedicalCase`.
- Create a static variable `next_id` set to 1000.

## Task 2: Auto-Incrementing
- Inside the constructor:
  - Increment the class variable first.
  - Assign the result to an instance variable `self.case_id`.

## Task 3: Integration
In `main()`:
1. Create three different cases.
2. Print each case's name and its auto-generated ID.
