# Lab 1 Tasks

## Task 1: Create the Class
- Define `WaitingRoom`.
- Initialize an empty dictionary `self.queue` in `__init__`.

## Task 2: Port Logic
- Move the logic from `add_patient` to `WaitingRoom.check_in`.
- Move the logic from `find_patient` to `WaitingRoom.get_wait_time`.

## Task 3: Verify
In `main()`:
1. Create an instance of the class.
2. Check in two patients.
3. Retrieve data for one of them.
