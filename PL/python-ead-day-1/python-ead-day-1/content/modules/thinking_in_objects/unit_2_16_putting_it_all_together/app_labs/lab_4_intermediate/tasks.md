# Lab 4 Tasks

## Task 1: Audit System
- Create `AuditLog` class.
- Add `log_entry(msg)`.

## Task 2: Account Security
- Create `PatientAccount`.
- Store `self._password` as protected/private.
- Implement `login(password, logger)`.

## Task 3: Interaction
In `main()`:
1. Initialize the logger.
2. Initialize an account (user="alice", pass="secret").
3. Attempt correct login (Observe log).
4. Attempt wrong login (Observe log).
