# Lab 2 Tasks

## Task 1: Subclassing
- Define `InfusionPump` inheriting from `MedicalDevice`.
- Define `HeartMonitor` inheriting from `MedicalDevice`.

## Task 2: Fulfilling the Contract
- Implement `operate()` in `InfusionPump`.
- Implement `operate()` in `HeartMonitor`.

## Task 3: Usage
In `main()`:
1. Create a instance of each.
2. Print the value returned by `operate()`.
3. Print the value returned by `get_status()`.
