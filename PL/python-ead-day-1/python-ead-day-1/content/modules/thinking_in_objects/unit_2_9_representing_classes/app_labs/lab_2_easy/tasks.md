# Lab 2 Tasks

## Task 1: Constructor Setup
- Define `MedicalDevice`.
- Implement `__init__(self, model, serial, dept)`.

## Task 2: State Assignment
- Map the arguments to `self.model`, `self.serial`, and `self.dept`.
- Hardcode `self.status = "Active"` inside the constructor.

## Task 3: Verification
- In `main()`, create a Ventilator and a Pump.
- Print their attributes to verify they are distinct.
