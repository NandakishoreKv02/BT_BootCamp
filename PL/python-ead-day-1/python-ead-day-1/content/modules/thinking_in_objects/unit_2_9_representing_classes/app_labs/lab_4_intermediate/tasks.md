# Lab 4 Tasks

## Task 1: Identify Violations
Look at the starter code and note the incorrect casing for classes and methods.

## Task 2: Apply PEP 8
- Rename the class to `SurgicalRobot`.
- Rename the constructor arguments to `robot_id`.
- Rename the method to `perform_calibration`.

## Task 3: Complete the Method
Inside the method, access `self.robot_id` to print "Calibration starting for Robot: ...".

## Task 4: Execute
Instantiate the robot in `main()` using the new class name and call the new method name.
