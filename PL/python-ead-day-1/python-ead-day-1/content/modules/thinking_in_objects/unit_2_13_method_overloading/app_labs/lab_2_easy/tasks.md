# Lab 2 Tasks

## Task 1: Conditional Method
- Define `VitalLogger`.
- Implement `log_hr(self, bpm, sensor=None)`.

## Task 2: Internal Branching
- Use an `if sensor is None` check to return different strings.

## Task 3: Testing Signatures
In `main()`:
1. Create a logger.
2. Call `log_hr(72)`.
3. Call `log_hr(85, "S-99")`.
4. Print both results.
