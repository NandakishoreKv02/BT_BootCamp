# Lab 1 Tasks

## Task 1: Method Definition
- Define `PatientRegistrar`.
- Implement `register(self, name, insurance="Self-Pay", ward=0)`.

## Task 2: Formatting
- Return a string showing the name, insurance, and ward.

## Task 3: Multi-call Test
In `main()`:
1. Create a registrar.
2. Call `register` with only one argument ("Alice").
3. Call `register` with three arguments ("Bob", "Medicare", 101).
4. Print the results.
