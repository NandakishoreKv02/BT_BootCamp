# Lab 4 Tasks

## Task 1: Dictionary Initialization
- Define `PatientProfile`.
- Initialize `self.tags = {}` in `__init__`.

## Task 2: Kwargs Capture
- Implement `add_tags(self, **kwargs)`.
- Use `self.tags.update(kwargs)`.

## Task 3: Reporting
- Implement a method to print all tags in a clean list.

## Task 4: Integration
In `main()`:
1. Create a patient.
2. Call `add_tags` with multiple named parameters.
3. Print the patient summary.
