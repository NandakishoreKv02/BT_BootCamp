# Lab 3 Tasks

## Task 1: The Variable Signature
- Define `LabAnalyzer`.
- Implement `average_glucose(self, *readings)`.

## Task 2: Math Logic
- Use `sum(readings)` and `len(readings)` to calculate the average.
- Add a guard clause for `len(readings) == 0`.

## Task 3: Demonstration
In `main()`:
1. Create an analyzer.
2. Call it with 2 values.
3. Call it with 4 values.
4. Print the averages.
