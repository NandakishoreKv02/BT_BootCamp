# Lab 2 Tasks

## Task 1: Base Tool
- Define `MedicalTool`.
- Implement `use(self)` returning a generic string.

## Task 2: Override Tool
- Define `Stethoscope` inheriting from `MedicalTool`.
- Override `use(self)` returning a specialized string.

## Task 3: Integration
In `main()`:
1. Instantiate both.
2. Print the results of their `use()` calls.
