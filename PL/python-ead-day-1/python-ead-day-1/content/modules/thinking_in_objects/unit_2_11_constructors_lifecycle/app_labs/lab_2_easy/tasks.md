# Lab 2 Tasks

## Task 1: Constructor with Defaults
- Define `LabRequest`.
- Implement `__init__(self, test_name, priority="Routine")`.

## Task 2: State Assignment
- Map arguments to instance attributes.

## Task 3: Varied Instantiation
In `main()`:
1. Create `req1` using only one argument.
2. Create `req2` using both arguments.
3. Print results to observe the default logic in action.
