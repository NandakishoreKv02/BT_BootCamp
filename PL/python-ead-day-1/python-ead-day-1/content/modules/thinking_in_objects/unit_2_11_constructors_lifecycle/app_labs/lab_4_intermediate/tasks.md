# Lab 4 Tasks

## Task 1: Constructor Definition
- Define `VitalsReading`.
- `__init__(self, patient_name, heart_rate)`:
  - Store basic attributes.

## Task 2: Internal Logic
Inside the constructor:
- Use an `if-else` block to evaluate the `heart_rate`.
- Set `self.critical_alert` accordingly.

## Task 3: Instantiation
In `main()`:
1. Create a reading for 75 bpm.
2. Create a reading for 120 bpm.
3. Print the alert status for both.
