# Lab 1 Tasks

## Task 1: Constructor Design
- Define `TriageRecord`.
- Implement `__init__(self, patient_name, symptom)`.

## Task 2: State Assignment
- Map the arguments to `self.patient_name` and `self.symptom`.

## Task 3: Instantiation
In `main()`:
1. Create a `TriageRecord` with required data.
2. Print a formatted message showing the attributes.
