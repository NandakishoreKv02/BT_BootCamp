# Lab 2 Tasks: The Constructor and Self

## Task 1: Update the Patient Class
- Open `starter_code.py`.
- Add an `__init__` method to the `Patient` class.
- The method should take `self`, `name`, and `age` as parameters.
- Inside `__init__`, assign `name` to `self.name` and `age` to `self.self.age`.

## Task 2: Create a Validated Instance
- Create a `Patient` object named `patient1` with the name "John Doe" and age 30.
