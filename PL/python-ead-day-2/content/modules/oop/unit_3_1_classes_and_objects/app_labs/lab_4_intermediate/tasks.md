# Lab 4 Tasks: Managing Multiple Instances

## Task 1: Update the Patient Class
- Open `starter_code.py`.
- Ensure the `Patient` class takes `name` and `condition` (e.g., "Stable", "Critical") in its `__init__` constructor.

## Task 2: Create the Census
- Create 4 instances of `Patient` with different names and conditions.
- Store them in a list named `ward_census`.

## Task 3: Generate the Report
- Write a function `generate_report(patients)` that takes a list of patients and returns a list of strings in the format: "NAME: [name], CONDITION: [condition]".
