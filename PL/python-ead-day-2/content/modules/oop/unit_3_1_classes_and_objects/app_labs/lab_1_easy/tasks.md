# Lab 1 Tasks: Defining Classes and Objects

## Task 1: Define the Patient Class
- Open `starter_code.py`.
- Define a class named `Patient`.
- The class body can be `pass` for now.

## Task 2: Create Instances
- Outside the class definition, create two objects:
  - `patient1`: An instance of `Patient`.
  - `patient2`: Another instance of `Patient`.
